
#include <stdio.h>

int main(void)

{
    
    /*

    //読み込んだ整数値の符号を判定するプログラム
    
    int no;
    
    printf("整数を入力して下さい:");scanf("%d",&no);
    
    
    if(no==0)
        puts("その値は0です。");
    else if (no>0)
        puts("その値は正です。");
    else
        puts("その値は負です。");
    
    
    //他にも関係演算子は
    //<演算子　a<b aがbよりも小さければ1,そうでなければ0を返す。（その型はint型）
    //>演算子　a>b aがbよりも大きければ1,そうでなければ0を返す。（その型はint型）
    //<=演算子　a<=b aがbよりも小さいか等しければ1、そうでなければ0を返す。（その型はint型）
    //>=演算子　a>=b aがbよりも大きいか等しければ1、そうでなければ0を返す。（その型はint型）
    
    
    */
    
    
    
    //演習3-2等価演算子や関係演算子が1や0を生成することを確認するプログラム
    //評価が正しいものは１を返し、正しくないものは0を返す。
   
    /*
    
    int a,b,c;
    
    a=1;
    
    b=1;
    
    c=2;
    
    printf("%d\n",a==b);
    puts("1のはず");
    
    
    printf("%d\n",a==c);
    puts("0のはず");
    

    printf("%d\n",b==c);
    puts("0のはず");
    
    
    printf("%d\n",a>b);
    puts("0のはず");
   
    
    printf("%d\n",a<b);
    puts("0のはず");
    
    
    printf("%d\n",a>c);
    puts("0のはず");
    
    printf("%d\n",a<c);
    puts("1のはず");
    
    printf("%d\n",b<c);
    puts("1のはず");
    
    printf("%d\n",b>c);
    puts("0のはず");
    
    printf("%d\n",a<=b);
    puts("1のはず");
    
    printf("%d\n",a>=b);
    puts("1のはず");
    
    printf("%d\n",a<=c);
    puts("1のはず");
    
    printf("%d\n",a>=c);
    puts("0のはず");
    
    printf("%d\n",b<=c);
    puts("1のはず");
    
    printf("%d\n",b>=c);
    puts("0のはず");
    
     */
    
    
    
    
    
    //演習3-3　絶対値を表示するプログラム
    
    /*
    
    int a;
    
    printf("整数を入力して下さい:");scanf("%d",&a);
    
    if(a>=0)
       printf("絶対値は%dです。\n",a);
    else if(a<=0)
        printf("絶対値は%dです。\n",a*(-1));
    */
    
    
    /*
    
    
    //演習3-4 ふたつの整数値を読み込んで、大小関係の結果を表示するプログラム
    
    int a,b;
    
    puts("二つの整数を入力してください。");
    printf("整数A:");scanf("%d",&a);
    printf("整数B:");scanf("%d",&b);

    if(a==b)
        puts("AとBは等しいです。");
    else if(a>b)
        puts("AはBより大きいです。");
    else if(a<b)
        puts("AはBより小さいです。");
    
    //else if　という構文がある訳ではない。
    //if文は一種の文であり、elseが制御する文としてif文がかかっているという構図なだけ。
     
     
     */
    
    
    /*
    
    //大きい方の値を求める
    
    int n1,n2;
    
    puts("二つの整数を入力してください。");
    

    printf("整数1:");scanf("%d",&n1);
    printf("整数2:");scanf("%d",&n2);
    
    if(n1>n2)
        printf("大きい方の値は%dです。\n",n1);
    else
        printf("大きい方の値は%dです。\n",n2);
    
    
    */
    
    
    
    /*
    
    //大きい方の値を求める その2
    
    int n1,n2,max;
    
    puts("二つの整数を入力してください。");
    
    printf("整数1:");scanf("%d",&n1);
    printf("整数2:");scanf("%d",&n2);
    
    if(n1>n2)
        max=n1;
    else
        max=n2;
    
        printf("大きい方の値は%dです。\n",max);
    
    
    //maxに格納することで、後々に値を利用するには融通が利く。
    
     */
    
    
    /*
    
    //三値の最大値を求める
    
    
    int n1,n2,n3,max;
    
    puts("３つの整数を入力してください。");
    
    printf("整数1:");scanf("%d",&n1);
    printf("整数2:");scanf("%d",&n2);
    printf("整数3:");scanf("%d",&n3);
    
    max=n1;
    
    if(n2>max)
        max=n2;
    if(n3>max)
        max=n3;
    
    //if文の連続使用がポイント
    
    printf("最大値は%dです。\n",max);

     */
    
    
    /*
    
    
    //演習3-5 ３つの整数値を読み込んで最小値を表示する。
    
    int n1,n2,n3,min;
    
    puts("３つの整数を入力してください。");
    
    printf("整数1:");scanf("%d",&n1);
    printf("整数2:");scanf("%d",&n2);
    printf("整数3:");scanf("%d",&n3);
    
    min=n1;
    
    if(n2<min)
        min=n2;
    if(n3<min)
        min=n3;
    
    //if文の連続使用がポイント
    
    printf("最小値は%dです。\n",min);

    */
    
    
    
    
    
    
    //演習3-6 ４つの整数値を読み込んで、その最大値を求めて表示するプログラム
    
    
    
    int n1,n2,n3,n4,max;
    
    puts("4つの整数を入力してください。");
    
    printf("整数1:");scanf("%d",&n1);
    printf("整数2:");scanf("%d",&n2);
    printf("整数3:");scanf("%d",&n3);
    printf("整数4:");scanf("%d",&n4);

    
    max=n1;
    
    if(n2>max)
        max=n2;
    if(n3>max)
        max=n3;
    if(n4>max)
        max=n4;
    
    //if文の連続使用がポイント
    
    printf("最大値は%dです。\n",max);

    
    
    
    
    
    
    
    
    
    return 0;
}


//if文系は、条件を満たせば1を返す。満たさなければ0を返す。
//0を返すとは、何も出力をしないということ。






