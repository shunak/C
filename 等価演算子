#include <stdio.h>

int main(void)

{
//読み込んだ二つの整数値は等しいか
 
    /*
     int x1, x2;
    
    printf("二つの整数を入力して下さい。\n");
    printf("整数A:");scanf("%d",&x1);
    printf("整数B:");scanf("%d",&x2);
    
    if(x1==x2)//==演算子は、左右のオペランドの値を比較して、それらが等しければ1を、そうでなければ0という値を生成する。
        //（その型は整数であるint型）この場合、式x1==x2を評価して、それらの値が等しければ値は1となり、そうでなければ0となる。
        puts("それらの値は同じです。");
    else
        puts("それらの値は違います。");
     */
    
    
    
    /*
    //上記プログラムを論理否定演算子!=を用いて書き換え
    
    int x1, x2;
    
    printf("二つの整数を入力して下さい。\n");
    printf("整数A:");scanf("%d",&x1);
    printf("整数B:");scanf("%d",&x2);
    
    if(x1!=x2)//!=演算子は、左右のオペランドの値を比較して、それらが等しくなければ1を、そうでなければ0という値を生成する。
        //（その型は整数であるint型）この場合、式x1!=x2を評価して、それらの値が等しくなければ値は1となり、そうでなければ0となる。
        puts("それらの値は違います。");
    else
        puts("それらの値は同じです。");
    */
    
    
    //剰余を比較
    //最下位の桁は5であるか
    
    int vx;
    
    printf("整数を入力して下さい:");scanf("%d",&vx);
    
    if(vx%10==5)//==演算子は、左右のオペランドの値を比較して、それらが等しければ1を、そうでなければ0という値を生成する。
        //（その型は整数であるint型）この場合、式x1==x2を評価して、それらの値が等しければ値は1となり、そうでなければ0となる。
        puts("最も下の桁は5です。");


    return 0;
}

